# 🛡️ SafeNav — Навігатор стійкий до РЕБ

Форк OsmAnd з додатковими модулями для України.

## Проблема

Під час повітряних тривог в Україні вмикається РЕБ (радіоелектронна боротьба),
який глушить GPS сигнал. Стандартні навігатори (Google Maps, Waze, OsmAnd)
при цьому повністю втрачають позицію або показують хибне місцезнаходження.

## Рішення SafeNav

### Модулі що додаються до OsmAnd:

#### 🚨 alerts/ — Повітряні тривоги
- Реальний час з alerts.in.ua API
- Показ активних тривог на карті
- Укриття з бази OSM (amenity=shelter)
- TTS сповіщення: *"Увага! Тривога в Київській області"*
- Автоматичний перехід в офлайн режим при тривозі

#### 📡 reb/ — Захист від РЕБ
- **GpsValidator** — детектує GPS спуфінг:
  - Порівнює швидкість GPS vs IMU акселерометр
  - Перевіряє стрибки позиції (> 5км/с = РЕБ)
  - Аналізує точність GPS сигналу
- **ImuDeadReckoning** — навігація без GPS:
  - Фільтр Мадгвіка (злиття гіроскоп + акселерометр + компас)
  - Dead reckoning від останньої відомої точки
  - Показ оціночної похибки на карті
  - Автокорекція при зупинках (Zero Velocity Update)

#### 🧠 ml/ *(в розробці)*
- TFLite модель навчена на реальній їзді
- Вхід: IMU дані (50 кроків × 10 датчиків)
- Вихід: Δlat, Δlon, heading
- Навчання на власних даних телефону → висока точність

## Статуси навігації

```
🟢 GPS надійний         — звичайна навігація OsmAnd
🟡 GPS підозрілий       — попередження + IMU паралельно
🔴 РЕБ виявлено         — повний перехід на IMU
⚫ Немає сигналу        — тільки IMU dead reckoning
```

## Точність IMU режиму

| Час без GPS | Похибка |
|---|---|
| 0–30 сек | < 2м ✅ |
| 30 сек – 2 хв | < 10м ✅ |
| 2–5 хв | < 30м ⚠️ |
| 5+ хв | зростає (корекція по знаках) |

## Структура проекту

```
OsmAnd/src/net/osmand/plus/safenav/
├── alerts/
│   ├── AlertsApiClient.kt    ← API alerts.in.ua
│   └── AlertsPlugin.kt       ← Плагін карти
├── reb/
│   ├── GpsValidator.kt       ← Детектор РЕБ
│   └── ImuDeadReckoning.kt   ← Dead reckoning
└── ml/                       ← (в розробці)
    ├── SignDetector.kt
    └── DriftCorrector.kt
```

## Збір тренувальних даних

Режим запису треків (в розробці):
- Паралельний запис GPS + IMU під час звичайної їзди
- Формат: CSV (timestamp, lat, lon, accel_x/y/z, gyro_x/y/z, mag_x/y/z)
- Навчання моделі на ПК: `python train.py --data ./tracks/`
- Розгортання: model.tflite → в додаток

## Розробник

TehnoDron — [tehnodron.in.ua](https://tehnodron.in.ua)
Telegram: [@tehnodronua](https://t.me/tehnodronua)
